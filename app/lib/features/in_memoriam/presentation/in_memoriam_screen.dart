import 'package:flutter/material.dart';

import '../../common/placeholder_screen.dart';

class InMemoriamScreen extends StatelessWidget {
  const InMemoriamScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderScreen(title: 'En mémoire', phase: 4);
  }
}
